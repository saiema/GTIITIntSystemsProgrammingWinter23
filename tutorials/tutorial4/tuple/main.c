/*
* A simple program that takes, from the command-line, a sequence of integer values as input
* and returns the minimum and maximum values within the provided values.
* Usage: ./min_max <integer values>
*/

#include <stdio.h>
#include <tuple.h>


/*
* Given a non-empty array, it will return a tuple containing
* the minimum element of the array as the first element,
* and the maximum element as second.
*/
tuple min_max(const int size, const int values[size]);

int main(int argc, char ** argv) {
    //TODO: complete
}

tuple min_max(const int size, const int values[size]) {
    //TODO: complete
}