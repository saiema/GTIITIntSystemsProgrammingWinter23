/*
* Using a List of int (list_int), this program will take int values from the command line
* It will create a list with those elements (in the received order)
* It will then print the list and print the maximum value in that list
* Any allocated memory must be freed before the program terminates.
*/

#include <list_int.h>
#include <boolean.h>
#include <stdio.h>

int main(int argc, char ** argv) {
    //TODO: implement
}